$(document).ready(function(){
	// make the tabs
	$(document).find('.b-tab').each(function(){
		let self = $(this);
		console.log(self.attr('data-tab-title'));
	})
	// make the middle tab selected and visible
	// handle tab click
})